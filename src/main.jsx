import React from "react";
import ReactDOM from "react-dom/client";
import Codebooker from "./codebooker.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Codebooker />
  </React.StrictMode>
);
